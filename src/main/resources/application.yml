server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/db_espace
    username: root
    password: 123456
  jpa:
    database: mysql
    open-in-view: true
    show-sql: true

jwt:
  config:
    key: zephon-espace
    ttl: 3600000

# 开启Swagger
swagger:
  enabled: true